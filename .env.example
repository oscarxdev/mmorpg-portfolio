# GitHub Personal Access Token (classic) con scope `public_repo`
# Genera uno en: https://github.com/settings/tokens
# Sin token, la API tiene un rate limit muy bajo (60 req/h por IP)
GITHUB_TOKEN=ghp_your_token_here
